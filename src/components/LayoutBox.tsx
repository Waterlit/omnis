'use client'

import { Box, BoxProps, styled } from '@mui/material'

const LayoutBox = styled(Box)<BoxProps>(() => ({
    background: 'linear-gradient(57.67deg, #200D40 0%, #040218 69.38%)',
    minHeight: '100vh',
}))

export default LayoutBox
